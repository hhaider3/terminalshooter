[package]
name = "terminalshooter"
version = "0.2.0"
edition = "2024"
description = "TERMINAL // BREACH — a five-wave terminal arena FPS"
publish = false

[dependencies]
crossterm = "0.29"
ctrlc = { version = "3.5", features = ["termination"] }

[dev-dependencies]
libc = "0.2"

[profile.release]
lto = "thin"
codegen-units = 1
